# Runtime dependencies are declared in pyproject.toml.
# To install surpyval with all dependencies:
#   pip install -e .
#
# For development (includes test and dev tools):
#   pip install -r requirements_dev.txt
