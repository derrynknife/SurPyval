Success Run
===========

The lower confidence bound on the per-trial success probability after a
run of ``n`` successes with no failures -- the zero-failure
demonstration test. Import it as ``surpyval.success_run``.

.. automodule:: surpyval.univariate.nonparametric.success_run
   :members:
